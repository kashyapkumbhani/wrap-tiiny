# Tinny Environment Variables
# Copy this file to .env and update the values

# Server Configuration
PORT=3000
NODE_ENV=development

# Session Security (REQUIRED - change this!)
SESSION_SECRET=change_this_long_random_string_in_production

# Database Configuration
DB_TYPE=sqlite
SQLITE_PATH=./data/metadata.db

# Domain Configuration
BASE_DOMAIN=lvh.me

# Upload Limits
MAX_HTML_BYTES=5000000
MAX_ZIP_BYTES=25000000

# Optional: PostgreSQL (for production)
# POSTGRES_URL=postgres://user:pass@host:5432/dbname

# Optional: Storage Backend (Phase 1.5+)
# STORAGE_BACKEND=local
# S3_BUCKET=your-bucket
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_ACCESS_KEY_ID=your-access-key
# S3_SECRET_ACCESS_KEY=your-secret-key

# Optional: SMTP Configuration (Phase 1.5+)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password